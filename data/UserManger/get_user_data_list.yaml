# 公共参数
case_common:
  allureEpic: 商城平台接口
  allureFeature: 用户管理
  allureStory: 获取用户数据列表

get_user_data_list_01:
    host: ${{host()}}
    url: /api/private/v1/users
    method: GET
    detail: 获取用户数据列表成功
    headers:
        Authorization: $cache{token}
    # 请求的数据，是 params 还是 json、或者 data, file
    requestType: params
    # 是否执行，空或者 true 都会执行
    is_run: 
    data:
        pagenum: 1
        pagesize: 20
    assert:
        # 断言接口状态码
        errorStatus:
            jsonpath: $.meta.status
            type: ==
            value: 200
            AssertType:
        # 断言接口返回的msg
        errorInfo:
            jsonpath: $.meta.msg
            type: ==
            value: "获取管理员列表成功"
            AssertType:

get_user_data_list_02:
    host: ${{host()}}
    url: /api/private/v1/users
    method: GET
    detail: 获取用户数据列表失败（pagenum为空）
    headers:
        Authorization: $cache{token}
    # 请求的数据，是 params 还是 json、或者 data, file
    requestType: params
    # 是否执行，空或者 true 都会执行
    is_run:
    data:
        pagenum:
        pagesize: 20
    assert:
        # 断言接口状态码
        errorStatus:
            jsonpath: $.meta.status
            type: ==
            value: 400
            AssertType:
        # 断言接口返回的msg
        errorInfo:
            jsonpath: $.meta.msg
            type: ==
            value: "pagenum 参数错误"
            AssertType:

get_user_data_list_03:
    host: ${{host()}}
    url: /api/private/v1/users
    method: GET
    detail: 获取用户数据列表失败（pagesize为空）
    headers:
        Authorization: $cache{token}
    # 请求的数据，是 params 还是 json、或者 data, file
    requestType: params
    # 是否执行，空或者 true 都会执行
    is_run:
    data:
        pagenum: 1
        pagesize:
    assert:
        # 断言接口状态码
        errorStatus:
            jsonpath: $.meta.status
            type: ==
            value: 400
            AssertType:
        # 断言接口返回的msg
        errorInfo:
            jsonpath: $.meta.msg
            type: ==
            value: "pagesize 参数错误"
            AssertType:

get_user_data_list_04:
    host: ${{host()}}
    url: /api/private/v1/users
    method: GET
    detail: 获取用户数据列表失败（多出abc参数）
    headers:
        Authorization: $cache{token}
    # 请求的数据，是 params 还是 json、或者 data, file
    requestType: params
    # 是否执行，空或者 true 都会执行
    is_run:
    data:
        pagenum: 1
        pagesize: 20
        abc: 100
    assert:
        # 断言接口状态码
        errorStatus:
            jsonpath: $.meta.status
            type: ==
            value: 200
            AssertType:
        # 断言接口返回的msg
        errorInfo:
            jsonpath: $.meta.msg
            type: ==
            value: "获取管理员列表成功"
            AssertType:

get_user_data_list_05:
    host: ${{host()}}
    url: /api/private/v1/users
    method: GET
    detail: 获取用户数据列表失败（缺少pagenum参数）
    headers:
        Authorization: $cache{token}
    # 请求的数据，是 params 还是 json、或者 data, file
    requestType: params
    # 是否执行，空或者 true 都会执行
    is_run:
    data:
        pagesize: 20
    assert:
        # 断言接口状态码
        errorStatus:
            jsonpath: $.meta.status
            type: ==
            value: 400
            AssertType:
        # 断言接口返回的msg
        errorInfo:
            jsonpath: $.meta.msg
            type: ==
            value: "pagenum 参数错误"
            AssertType:

get_user_data_list_06:
    host: ${{host()}}
    url: /api/private/v1/users
    method: GET
    detail: 获取用户数据列表失败（修改pagenum参数）
    headers:
        Authorization: $cache{token}
    # 请求的数据，是 params 还是 json、或者 data, file
    requestType: params
    # 是否执行，空或者 true 都会执行
    is_run:
    data:
        num: 1
        pagesize: 20
    assert:
        # 断言接口状态码
        errorStatus:
            jsonpath: $.meta.status
            type: ==
            value: 400
            AssertType:
        # 断言接口返回的msg
        errorInfo:
            jsonpath: $.meta.msg
            type: ==
            value: "pagenum 参数错误"
            AssertType:

get_user_data_list_07:
    host: ${{host()}}
    url: /api/private/v1/users
    method: GET
    detail: 获取用户数据列表失败（没有参数）
    headers:
        Authorization: $cache{token}
    # 请求的数据，是 params 还是 json、或者 data, file
    requestType: params
    # 是否执行，空或者 true 都会执行
    is_run:
    data:
    assert:
        # 断言接口状态码
        errorStatus:
            jsonpath: $.meta.status
            type: ==
            value: 400
            AssertType:
        # 断言接口返回的msg
        errorInfo:
            jsonpath: $.meta.msg
            type: ==
            value: "pagenum 参数错误"
            AssertType: